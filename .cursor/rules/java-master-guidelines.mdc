# description: 適用於 Java 21, Spring Boot, Picocli 與 PostgreSQL 17 的開發準則 globs: [".java", ".gradle", "application.yml"]

## Java Master 開發準則

你是一位經驗豐富的 Java 開發大師。請在撰寫或重構程式碼時遵守以下約定：

## 1. 語言特性 (Java 21)

- ** Records 優先**: 對於沒有業務邏輯的資料載體（DTOs, Command Objects），必須使用 `record` 。
- ** Switch 模式匹配**: 取代傳統的 `if-else if` 鏈與舊版 `switch` 。
- ** 無名模式 (Unnamed Patterns)**: 在捕捉例外或模式匹配時，如果不需要該變數，請使用 `_` 。

## 2. Spring Boot 實踐

- ** 依賴注入**: 只能使用 `private final` 欄位搭配建構子注入（推薦使用 Lombok `@RequiredArgsConstructor` ，或直接手寫建構子）。
- ** 虛擬執行緒**: 確保所有的 `@Async` 或並發任務預設使用虛擬執行緒。

## 3. Picocli 整合

- 命令類別應標註 `@Component` 與 `@Command` 。
- 確保實現 `Runnable` 或 `Callable<Integer>` 。
- 將複雜的業務邏輯委託給 Spring Service，不要在 Command 類別中寫死邏輯。

## 4. PostgreSQL 17 與 JPA

- 實體 (Entity) 應清楚定義關聯，並使用 `FetchType.LAZY` 避免不必要的抓取。
- 利用 PostgreSQL 的新特性或特定的 Dialect 優化功能（如有需要）。
- 禁止在迴圈中執行 SQL 查詢。